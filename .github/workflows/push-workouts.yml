# TrainingPeaks workflow — ikke i brug. Intervals.icu er master.
# Brug build-workouts.yml i stedet.
name: UDGÅET - Push workouts til TrainingPeaks
on:
  workflow_dispatch:
jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Ikke i brug - se build-workouts.yml"
